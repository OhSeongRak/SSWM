node {
     environment {
        DOCKER_IMAGE = 'node:18.16.1' // Node.js 버전 18.16.1 이미지를 사용합니다.
        GIT_REPO_URL = 'https://a206:MdQZeq3wYF45cw6yCAQ1@lab.ssafy.com/s09-webmobile1-sub2/S09P12A206.git'
        GIT_BRANCH = 'deploy-frontend' // 빌드할 Git 브랜치
    }
    stage('Checkout') {
        sh "echo start checkout"
        git url: GIT_REPO_URL, branch: 'deploy-frontend'
        sh "echo checkout done"
    }

    stage('Build') {
        sh "echo start building ,,"
        sh "cd frontend/sswm-front"

        dir ('frontend') {
            dir ('sswm-front') {
                sh "pwd"
                sh ""

            }
        }
        
        sh "echo build finished"
    }

    stage ('unit test') {

    }

    stage('Deploy') {
        // sh "echo deployment not yet determined"
    }
}

