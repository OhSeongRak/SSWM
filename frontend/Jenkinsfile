pipeline {
    agent {
        
    }
     stages {
        stage('Checkout') {
            steps {
               
            }
        }
     
    }
    post {
        success {
            // Actions to perform if the pipeline is successful
        }
        failure {
            // Actions to perform if the pipeline fails
        }
    }
    
}
